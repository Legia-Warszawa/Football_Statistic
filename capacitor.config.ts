import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.footballstatistic',
  appName: 'footballstatistic',
  webDir: 'dist'
};

export default config;
